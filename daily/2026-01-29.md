# 2026-01-29 학습 기록

## 📖 오늘 학습한 내용
- Fluent Python Ch2: An Array of Sequences (p.80-80)
- 소요 시간: 30분

## 💡 핵심 요약
- Unpacking
  - Tuple Unpacking
    ```python
    lax_coordinates = (33.9425, -118.408056)
    latitude, longitude = lax_coordinates
    ```
  - Without temporal variables
    ```python
    b, a = a, b
    ```
  - Function call
    ```python
    divmod(20, 8) # (2, 4)
    t = (20, 8)
    divmod(*t) # (2, 4)
    quotient, remainder = divmod(*t)
    ```

- Parallel assignment
  - Variables
    ```python
    a, b, *rest = range(5) # 0, 1, [2, 3, 4]
    a, b, *rest = range(3) # 0, 1, [2]
    a, b, *rest = range(2) # 0, 1, []
    a, *body, c, d = range(5) # 0, [1, 2], 3, 4
    head, b, c, d = range(5) # [0, 1], 2, 3, 4
    ```
  - Function call
    ```python
    def fun(a, b, c, d, *rest):
    	return a, b, c, d, rest

    fun(*[1, 2], 3, *range(4, 7)) # 1, 2, 3, 4, (5, 6)
    ```
  - Literals
    ```python
    *range(4), 4 # (0, 1, 2, 3, 4)
    [*range(4), 4] # [0, 1, 2, 3, 4]
    {*range(4), 4, *(5, 6, 7)} # {0, 1, 2, 3, 4, 5, 6, 7}
    ```

- Nested Unpacking
  - Tuple도 내포 가능하며, 표현식이 중첩 구조체와 일치하면 제대로 처리가 가능함
    ```python
    metro_areas = [
    	('Tokyo', 'JP', 36.933, (35.689722, 139.691667)),
    	('Delhi NCR', 'IN', 21.935, (28.613889, 77.208889)),
    	('Mexico City', 'MX', 20.142, (19.433333, -99.133333)),
    	('New York-Newark', 'US', 20.104, (40.808611, -74,020386)),
    	('Sao Paulo', 'BR', 19.649, (-23.547778, -46.635833)),
    ]
  
    def main():
        print(f'{"":15} | {"latitude":>9} | {"longitude":>9}')
        for name, _, _, (lat, lon) in metro_areas:
            if lon <= 0:
                print(f'{name:15} | {lat:>9.4f} | {lon:>9.4f}')
  
    if __name__ == '__main__':
        main()
    ```

- Unpacking assignment in List
  - 보통 잘 사용하지 않지만, 단일 레코드를 반환하는 쿼리가 있을 때(SQL 쿼리에 LIMIT 1 절이 있는 경우 등) 결과가 하나가 있음을 보장하는 코드로써 활용 가능
    ```python
    [record] = query_returning_single_row()
    ```

## 📝 상세 노트
→ [Chapter 2 정리본](../chapters/ch02_an_array_of_sequences/README.md) 참고

## ✅ 완료
- [x] Ch2 읽기
- [x] 예제 코드 실행
- [ ] Deep Dive
