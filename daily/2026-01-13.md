# 2026-01-13 학습 기록

## 📖 오늘 학습한 내용
- Fluent Python Ch2: An Array of Sequences (p.74-80)
- 소요 시간: 30분

## 💡 핵심 요약
- tuple
  - 레코드로서의 tuple
    ```python
    # tuple Unpacking
    city, year, pop, chg, area = ('Tokyo', 2003, 32_450, 0.66, 8014)

    # tuple Unpacking
    traveler_ids = [('USA', '31195855'), ('BRA', 'CE342567'), ('ESP', 'XDA205856')]
    for passport in sorted(traveler_ids):
        print('%s/%s' % passport)
    
    for country, _ in traver_ids:
        print(country)
    ```

    ```
    BRA/CE342567
    ESP/XDA205856
    USA/31195855

    USA
    BRA
    ESP
    ```

  - 불변 리스트로서의 tuple
    - 장점
      - 코드 안에 tuple이 보이면 그 리스트의 길이가 절대 바뀌지 않음을 알 수 있다 → 명확성
      - tuple은 똑같은 항목을 담은 list보다 메모리를 적게 소비함 → 성능(파이썬 인터프리터의 최적화 가능)
    - 주의사항
      - tuple 자체는 불변형이지만, 이는 tuple이 항상 동일한 객체를 참조한다는 의미임 → tuple이 참조하는 객체가 list 등과 같은 가변형이라면 참조된 객체의 내용은 변경가능 (버그 가능성 높음)
      - tuple을 포함한 어떤 객체에 고정된 값이 있는지 명시적으로 확인하려면 `hash`() 내장함수 응용을 통해 확인이 가능함
        ```python
        def fixed(o):
            try:
                hash(o)
            except TypeError:
                return False
            return True

        tf = (10, 'alpha', (1, 2))
        tm = (10, 'alpha', [1, 2])

        print(fixed(tf))
        print(fixed(tm))
        ```

        ```
        True
        False
        ```

## 📝 상세 노트
→ [Chapter 2 정리본](../chapters/ch02_an_array_of_sequences/README.md) 참고

## ✅ 완료
- [x] Ch2 읽기
- [x] 예제 코드 실행
- [ ] Deep Dive
